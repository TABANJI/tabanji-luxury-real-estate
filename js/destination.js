(()=>{'use strict';const destinations=Array.isArray(window.TABANJI_DESTINATIONS)?window.TABANJI_DESTINATIONS:[],properties=Array.isArray(window.TABANJI_PROPERTIES)?window.TABANJI_PROPERTIES:[],params=new URLSearchParams(location.search),id=Number(params.get('id')),slug=String(params.get('slug')||''),destination=destinations.find(d=>(id&&d.id===id)||(slug&&d.slug===slug)),root=document.getElementById('destination-root'),t=k=>window.TabanjiI18n?.t(k)||k,esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const seo=d=>{const ar=window.TabanjiI18n?.language==='ar',name=ar?d.nameAr:d.name,desc=ar?d.shortDescriptionAr:d.shortDescription;document.title=`${name} Property Guide | TABANJI ESTATES`;document.querySelector('meta[name="description"]').content=desc;document.querySelector('meta[property="og:title"]').content=`${name} | TABANJI ESTATES`;document.querySelector('meta[property="og:description"]').content=desc;document.querySelector('meta[property="og:image"]').content=d.heroImage;document.querySelector('link[rel="canonical"]').href=location.href.split('#')[0];let node=document.getElementById('destination-schema');if(!node){node=document.createElement('script');node.type='application/ld+json';node.id='destination-schema';document.head.append(node)}node.textContent=JSON.stringify({'@context':'https://schema.org','@type':'Place',name,description:desc,image:d.gallery,address:{'@type':'PostalAddress',addressLocality:d.city,addressCountry:'LB'}})};const propertyCard=(p,ar)=>`<article class="location-property"><a href="property.html?id=${p.id}"><img src="${esc(p.mainImage)}" alt="${esc(ar?p.titleAr:p.title)}" loading="lazy"><h3>${esc(ar?p.titleAr:p.title)}</h3><p>${esc(ar?p.locationAr:p.location)}</p><strong>$${p.price.toLocaleString('en-US')}${p.pricePeriod?` / ${p.pricePeriod}`:''}</strong></a></article>`;const render=()=>{if(!destination){root.innerHTML=`<section class="destination-not-found"><div><p class="eyebrow">TABANJI ESTATES</p><h1>${t('destinationNotFound')}</h1><p>${t('destinationNotFoundCopy')}</p><a class="button button-gold" href="destinations.html">${t('backLocations')}</a></div></section>`;return}const ar=window.TabanjiI18n?.language==='ar',name=ar?destination.nameAr:destination.name,city=ar?destination.cityAr:destination.city,desc=ar?destination.descriptionAr:destination.description,lifestyle=ar?destination.lifestyleAr:destination.lifestyle,highlights=ar?destination.highlightsAr:destination.highlights,types=ar?destination.popularPropertyTypesAr:destination.popularPropertyTypes;let available=destination.propertyIds.map(id=>properties.find(p=>p.id===id)).filter(Boolean);if(!available.length)available=properties.filter(p=>p.city===destination.name||p.district===destination.name||p.city===destination.city).slice(0,6);const related=destinations.filter(d=>d.id!==destination.id).sort((a,b)=>Number(b.city===destination.city)-Number(a.city===destination.city)).slice(0,3);root.innerHTML=`<section class="destination-detail-hero" id="destination-hero"><img class="destination-hero-image" src="${esc(destination.heroImage)}" alt="${esc(name)}"><div class="container-wide"><nav class="breadcrumbs"><a href="index.html">${t('home')}</a><span>/</span><a href="destinations.html">${t('destinations')}</a><span>/</span><span>${esc(name)}</span></nav><p class="eyebrow">${esc(city)} / LEBANON</p><h1>${esc(name)}</h1><p>${esc(ar?destination.shortDescriptionAr:destination.shortDescription)}</p></div></section><section class="destination-overview"><div class="container-wide"><div class="destination-editorial"><div><p class="eyebrow">${t('overview')}</p><h2>${t('discover')} ${esc(name)}</h2></div><div><p>${esc(desc)}</p><p><strong>${t('lifestyle')}:</strong> ${esc(lifestyle)}</p><div class="type-list">${types.map(x=>`<span>${esc(x)}</span>`).join('')}</div></div></div><div class="highlight-grid">${highlights.map((x,i)=>`<div><i>0${i+1}</i><span>${esc(x)}</span></div>`).join('')}</div><div class="destination-gallery">${destination.gallery.map((src,i)=>`<img src="${esc(src)}" alt="${esc(name)} — ${i+1}" ${i?'loading="lazy"':''}>`).join('')}</div><div class="destination-map"><i data-lucide="map-pin"></i><span>${esc(destination.mapQuery)}</span></div></div></section><section class="available-section"><div class="container-wide"><div class="section-heading"><div><p class="eyebrow">TABANJI COLLECTION</p><h2>${t('availableProperties')}</h2></div></div><div class="property-location-grid">${available.length?available.slice(0,6).map(p=>propertyCard(p,ar)).join(''):`<p>${t('noAreaProperties')}</p>`}</div></div></section><section class="related-section"><div class="container-wide"><div class="section-heading"><div><p class="eyebrow">${t('continueExploring')}</p><h2>${t('relatedLocations')}</h2></div></div><div class="related-grid">${related.map(d=>`<article class="related-card"><a href="destination.html?slug=${encodeURIComponent(d.slug)}"><img src="${esc(d.heroImage)}" alt="${esc(ar?d.nameAr:d.name)}" loading="lazy"><h3>${esc(ar?d.nameAr:d.name)}</h3><p>${esc(ar?d.cityAr:d.city)}</p></a></article>`).join('')}</div></div></section><section class="destination-advisor-cta"><div><p class="eyebrow">TABANJI PRIVATE OFFICE</p><h2>${t('exploreWithAdvisor')}</h2><a class="button button-gold" href="contact.html?subject=Buying%20Property">${t('speakAdvisor')}</a></div></section>`;seo(destination);if(window.lucide)window.lucide.createIcons()};document.addEventListener('tabanji:languagechange',render);render()})();
